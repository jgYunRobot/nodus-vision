add_library(nodus_vision_runtime STATIC vision_application.cpp)
target_include_directories(nodus_vision_runtime PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(nodus_vision_runtime PUBLIC nodus_vision_config nodus_vision_provider_http nodus_vision_core)
target_compile_features(nodus_vision_runtime PUBLIC cxx_std_17)
target_compile_options(nodus_vision_runtime PRIVATE -Wall -Wextra)
